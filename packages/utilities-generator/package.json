{
  "name": "@kudra/utilities-generator",
  "version": "1.3.1",
  "description": "A @kudra/nuxt Type Generator That Provides Utility Types And Functions",
  "license": "MIT",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rimraf lib",
    "compile": "tsc",
    "build": "yarn run clean && yarn run compile",
    "pack": "yarn run build && yarn pack"
  },
  "peerDependencies": {
    "@kudra/nuxt": "^1.0.0"
  }
}
