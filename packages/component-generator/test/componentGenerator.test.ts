describe("@kudra/component-generator", () => {
  const name: string = "jacob";

  it.todo("should generate types for global components");

  it("should be jacob", () => {
    expect(name).toBe("jacob");
  });
});
